# Setup

From the repository root, run `Rscript scripts/setup/01_install_dependencies.R` once to install the R packages used by this dataset workflow. The script lists dependencies explicitly so the workflow can be prepared on another machine.

See [`scripts/`](../README.md) for run order and [the dataset guide](../../docs/dataset.md) for source requirements.
