# Shared capture-group rule

`excluded_capture_groups.csv` records capture groups excluded when constructing comparable nocturnal-catch data. Daily-coverage curation uses this rule, so the authoritative copy stays with the dataset. The separate `ngulia-analysis` repository reads this file from the dataset checkout.

Interpretation and downstream use are described in [the dataset guide](../../docs/dataset.md).
